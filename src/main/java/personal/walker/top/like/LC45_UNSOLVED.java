package personal.walker.top.like;

public class LC45_UNSOLVED {
    /**
     * 假设当前跳转的范围是[curBegin, curEnd]，curFarthest是[curBegin, curEnd]中所有点能到达的最远点。
     * 一旦当前点到达 curEnd，则触发另一个跳转，并将新的 curEnd 设置为 curFarthest，然后保持上述步骤
     *
     * example :       2,3,1,1,4
     * curFarthest是 : 2,5,3,4,7.....
     * 所以从下面看出，每次循环找到局部最优解即可
     * @param nums
     * @return
     */
    public int jump(int[] nums) {

        return 0;
    }
}
